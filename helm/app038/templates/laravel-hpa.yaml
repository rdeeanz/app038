{{- if and .Values.laravel.enabled .Values.laravel.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "app038.fullname" . }}-laravel
  labels:
    {{- include "app038.laravel.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "app038.fullname" . }}-laravel
  minReplicas: {{ .Values.laravel.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.laravel.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.laravel.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.laravel.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.laravel.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.laravel.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
      - type: Percent
        value: 50
        periodSeconds: 60
    scaleUp:
      stabilizationWindowSeconds: 0
      policies:
      - type: Percent
        value: 100
        periodSeconds: 15
      - type: Pods
        value: 2
        periodSeconds: 15
      selectPolicy: Max
{{- end }}

